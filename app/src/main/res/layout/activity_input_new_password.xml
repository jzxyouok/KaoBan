<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context="com.dezhou.lsy.projectdezhoureal.InputNewPassword">
    <include layout="@layout/backxml"></include>

    <LinearLayout
        android:layout_marginTop="50dp"
        android:id="@+id/input_new_password_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:background="@drawable/bg_edittext_normal">
        <EditText
            android:id="@+id/input_new_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="输入密码"
            android:padding="10dp"
            android:textColorHint="@color/light_gray"
            android:textColor="@color/black"
            android:background="@drawable/bg_edittext_normal"/>
    </LinearLayout>
    <LinearLayout
        android:id="@+id/input_second_password_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_below="@id/input_new_password_layout"
        android:background="@drawable/bg_edittext_normal">
        <EditText
            android:id="@+id/input_second_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:hint="再次输入密码"
            android:background="@drawable/bg_edittext_normal"
            android:textColor="@color/black"
            android:textColorHint="@color/light_gray"/>
    </LinearLayout>
    <Button
        android:layout_below="@id/input_second_password_layout"
        android:id="@+id/confirm_change_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="确认修改"
        android:layout_marginTop="30dp"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:background="@drawable/login_btn_selector"
        android:textColor="@color/white"/>
</LinearLayout>
